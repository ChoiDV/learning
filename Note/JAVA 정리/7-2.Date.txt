API - "날짜"
자바에서 날짜와 시간에 관련된 클래스들에는 
1. Calendar 클래스  

2. GregorianCalendar 클래스

3. Date 클래스 

4. SimpleDateFormat 클래스 등 이 있다.

자바에서 날짜를 가져올때 월(Month)는 0부터 시작이다. 1월은 0, 2월은 1 ... 12월은 11 이다.
또 요일을 가져올땐 week 를 사용해서 1=일요일, 2=월요일 ... 7 =토요일 이 된다.
자세한 시간과 날짜를 가져오는 법은 소스파일에 있다.
